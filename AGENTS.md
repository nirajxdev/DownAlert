Frontend is Vite + React in `frontend/`. Backend is Express in `backend/`. Worker is node-cron in `worker/`.
Email is the only alert channel (Resend). Payments are out of scope for MVP — plan is a DB flag.
