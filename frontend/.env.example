# Copy to .env and adjust. Never commit .env.
# Local dev:
VITE_API_URL=http://localhost:5000
# Production (Vercel frontend env var — must be your deployed backend URL, NOT localhost):
# VITE_API_URL=https://downalert-api.vercel.app
